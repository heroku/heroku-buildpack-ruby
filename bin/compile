#!/bin/bash

BIN_DIR=$(cd $(dirname $0); pwd)

# bootstrap ruby
ruby_dir=`$BIN_DIR/bootstrap $2`
echo $ruby_dir
function atexit {
  rm -rf $ruby_dir
}
trap atexit EXIT

$ruby_dir/bin/ruby $BIN_DIR/ruby_compile $@
