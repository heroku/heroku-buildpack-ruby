#!/usr/bin/env ruby

require 'pathname'

release = Pathname.new(ARGV.first).join("tmp/heroku-buildpack-release-step.yml")

if release.exist?
  puts release.read
else
  puts <<YML
addons: []
default_process_types:
YML
end
